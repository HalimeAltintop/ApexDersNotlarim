/* Ternary Operator */
System.debug(math.mod(13,2)==0 ? 'Even Number' : ' Odd Number');

System.debug(math.mod(12,2)==0 ? 'Even Number' : ' Odd Number');

/* True dönerse 1. opsiyonu yazdir, 2. secenek dogruysa 2. secenegi yazdir.
Dynamic Ternary : Variable ile kullanilan */